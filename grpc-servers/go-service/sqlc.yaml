version: "1"
packages:
  - name: "db"
    path: "db"
    queries: "./queries.sql"
    schema: "./schema.sql"
    engine: "mysql"
    emit_json_tags: true
    emit_prepared_queries: true
    emit_interface: true